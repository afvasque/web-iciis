<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @project.title %>
</p>

<p>
  <strong>Investigador responsable:</strong>
  <%= @project.researchers.first.name %>
</p>

<p> 
	<strong>Sitio de estudio:</strong>
  	<%= @project.study_sites.first.location %>
</p>

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
